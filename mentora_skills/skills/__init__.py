# Skills package — one subdirectory per skill (underscore-named).
# Skill directories with logic.py have their own __init__.py and are
# directly importable. Skills with only skills.md are plain directories.
